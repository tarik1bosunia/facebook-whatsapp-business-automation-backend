name: Deploy Django App to VPS

on:
  push:
    branches:
      - main  # ✅ Update if you deploy from a different branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ✅ Checkout Code
        uses: actions/checkout@v4

      - name: 🔐 Set up SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: 📦 Rsync project files to VPS
        run: |
          rsync -avz --delete -e "ssh -i ~/.ssh/id_ed25519" ./ \
          ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:/home/deploy/fbaserver/

      - name: 🚀 Deploy with Docker Compose on VPS
        run: |
          ssh -i ~/.ssh/id_ed25519 ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} << 'EOF'
            set -e
            cd /home/deploy/fbaserver

            echo "📥 Pulling latest images..."
            docker compose pull

            echo "🧹 Cleaning up old containers..."
            docker compose down --remove-orphans

            echo "🔧 Building and starting containers..."
            docker compose up --build -d

            echo "🧽 Pruning unused Docker images..."
            docker image prune -f
          EOF

      - name: 📂 Copy static and media files from container to Nginx path
        run: |
          ssh -i ~/.ssh/id_ed25519 ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} << 'EOF'
            set -e
            echo "📁 Creating directories for static and media..."
            sudo mkdir -p /var/www/fbaserver/static /var/www/fbaserver/media

            echo "📤 Copying static files from container..."
            docker cp fbaserver-container:/app/staticfiles /var/www/fbaserver/static

            echo "📤 Copying media files from container..."
            docker cp fbaserver-container:/app/media /var/www/fbaserver/media
          EOF
